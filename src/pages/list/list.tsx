import * as React from 'react';

interface IListProps {
}

const List: React.FunctionComponent<IListProps> = (props) => {
  return (
    <>
    list page works!!
    </>
  );
};

export default List;
